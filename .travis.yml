sudo: false
cache: bundler
language: ruby
script: 'ci/travis.rb'
before_install:
  - "echo 'gem: --no-ri --no-rdoc' > ~/.gemrc"
  - gem install bundler
env:
  - RAILS_ENV=test RACK_ENV=test
rvm:
  - 2.2.3
notifications:
  email: false
